package com.anthony.bookstore;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class BookstoreController {
        
    // UserServiceImpl us = new UserServiceImpl();

    // @GetMapping("/hello")
    // public String hello(@RequestParam(value = "name") String name,@RequestParam(value = "age") Integer age,@RequestParam(value = "address") String address) {

    //     User user = us.newUser(name,age,address);
    //     return String.format("Hello %s!", user.toString());
    // }

}
